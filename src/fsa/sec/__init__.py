"""SEC EDGAR access layer: HTTP client, rate limiting, response cache, endpoints.

Phase 1. See PLANNING.md Section 4 (compliance/rate limiting) and Section 2
(fetch-on-demand + revalidating cache design).
"""
